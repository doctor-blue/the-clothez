<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="@dimen/_space4dp"
        android:layout_marginTop="@dimen/_space4dp"
        android:orientation="vertical">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <com.google.android.material.imageview.ShapeableImageView
                android:id="@+id/img_product_item"
                android:layout_width="match_parent"
                android:layout_height="@dimen/_space256dp"
                android:scaleType="centerCrop"
                android:src="@drawable/img_woman"
                app:shapeAppearance="@style/rounded_corner" />

            <androidx.appcompat.widget.AppCompatImageView
                android:id="@+id/img_favorite"
                android:layout_width="@dimen/img_size_normal"
                android:layout_height="@dimen/img_size_normal"
                android:layout_gravity="bottom"
                android:layout_marginStart="@dimen/_space10dp"
                android:layout_marginBottom="@dimen/_space10dp"
                android:background="@drawable/bg_circle_white"
                android:padding="@dimen/_space4dp"
                android:src="@drawable/ic_heart" />
        </FrameLayout>

        <TextView
            android:id="@+id/txt_product_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_screen_small"
            android:fontFamily="@font/montserrat_medium"
            android:text="Neon tank"
            android:textColor="@color/navy_light"
            android:textSize="@dimen/text_size_16" />

        <TextView
            android:id="@+id/txt_product_price"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/_space4dp"
            android:layout_marginBottom="@dimen/_space4dp"
            android:fontFamily="@font/montserrat_medium"
            android:text="$29.99"
            android:textColor="@color/navy"
            android:textSize="@dimen/text_size_16" />

    </LinearLayout>
</layout>